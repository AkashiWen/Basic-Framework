ext {
    android = [
            compileSdkVersion: 31,
            minSdkVersion    : 21,
            targetSdkVersion : 31,
            versionCode      : 1,
            versionName      : '1.0',
    ]
    module = [
            // 组件状态切换
            is_application: true
    ]
}
