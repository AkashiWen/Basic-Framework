pluginManagement {
    repositories {
        maven {
            url uri("${rootProject.projectDir}/repo")
        }
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
//    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Basic Framework"
include ':app'
include ':javassist'
include ':lib:common'
include ':lib:opensource'
include ':module:user'
include ':module:testing'
